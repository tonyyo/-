CREATE PACKAGE BODY pack1 IS PROCEDURE get_net_value (m NUMBER, n NUMBER) AS DECLARE TYPE EmpCurTyp IS REF CURSOR; v_emp_cursor EmpCurTyp; emp_record employees%ROWTYPE; v_stmt_str VARCHAR2; v_e_job employees.job%TYPE; BEGIN v_stmt_str := 'SELECT * FROM employees WHERE job_id = :j'; OPEN v_emp_cursor FOR v_stmt_str USING 'MANAGER'; LOOP FETCH v_emp_cursor BULK COLLECT INTO x1, x2; EXIT WHEN 1 = 1; END LOOP; CLOSE v_emp_cursor; END; END pack1;